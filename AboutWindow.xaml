<Window x:Class="TinyBongo.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About DesktopKitty"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="White">
    <Window.Resources>
        <Style x:Key="AboutLabelStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Comic Sans MS" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#555555" />
            <Setter Property="Margin" Value="0,0,12,10" />
        </Style>
        <Style x:Key="AboutValueStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Comic Sans MS" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Margin" Value="0,0,0,10" />
        </Style>
    </Window.Resources>
    <Border BorderBrush="Black"
            BorderThickness="2"
            CornerRadius="14"
            Margin="8"
            Padding="20,16">
        <StackPanel MinWidth="280">
            <TextBlock Text="DesktopKitty"
                       FontFamily="Comic Sans MS"
                       FontWeight="Bold"
                       FontSize="18"
                       Foreground="Black"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,14" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" MinWidth="120" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Version" Style="{StaticResource AboutLabelStyle}" />
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="VersionValue" Style="{StaticResource AboutValueStyle}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Author" Style="{StaticResource AboutLabelStyle}" />
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="AuthorValue" Style="{StaticResource AboutValueStyle}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="License" Style="{StaticResource AboutLabelStyle}" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="MIT License" Style="{StaticResource AboutValueStyle}" />

                <TextBlock Grid.Row="3" Grid.Column="0" Text="GitHub" Style="{StaticResource AboutLabelStyle}" />
                <TextBlock Grid.Row="3" Grid.Column="1" Style="{StaticResource AboutValueStyle}">
                    <Hyperlink NavigateUri="https://github.com/MayankArambhi/DesktopKitty"
                               RequestNavigate="GitHubLink_RequestNavigate">
                        Repository
                    </Hyperlink>
                </TextBlock>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="RAM Usage" Style="{StaticResource AboutLabelStyle}" />
                <TextBlock Grid.Row="4" Grid.Column="1" x:Name="RamValue" Style="{StaticResource AboutValueStyle}" />

                <TextBlock Grid.Row="5" Grid.Column="0" Text=".NET Version" Style="{StaticResource AboutLabelStyle}" Margin="0,0,0,0" />
                <TextBlock Grid.Row="5" Grid.Column="1" x:Name="DotNetValue" Style="{StaticResource AboutValueStyle}" Margin="0,0,0,0" />
            </Grid>

            <Button Content="Close"
                    Width="90"
                    Height="28"
                    Margin="0,16,0,0"
                    HorizontalAlignment="Center"
                    FontFamily="Comic Sans MS"
                    Click="CloseButton_Click" />
        </StackPanel>
    </Border>
</Window>
